{
  "version": 2,
  "buildCommand": "python3.9 scripts/fetch_company_data.py && next build",
  "installCommand": "python3.9 -m pip install simfin pandas && npm install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "routes": [
    {
      "src": "/api/(.*)",
      "headers": {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Methods": "GET, OPTIONS",
        "Access-Control-Allow-Headers": "X-Requested-With, Content-Type, Accept"
      }
    }
  ]
} 